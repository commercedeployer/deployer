# Secrets and local env
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Logs and OS
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Test artifacts (Playwright, manual QA)
test-artifacts-e2e/
test-artifacts-*/
test-artifacts-ui-check.png

# `templates/` — производная рабочая папка (mount-дериватив).
# Реальный источник правды: `templates-bundled/`.
# В рабочей папке могут появляться JSON из UI/API и QA-тесты — не коммитим.
templates/
!templates/.gitkeep

# Runtime data (MCP keys, etc.)
data/

# Coverage
coverage/
.nyc_output/

# IDE (local only)
.vscode/
.idea/
.cursorignore
.cursor/
